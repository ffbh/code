#include <iostream>
#include <fstream>
using namespace std;

int main(){
	ifstream in("C:\\temp.txt");
	int N;
	int a, b;
	char c;
	while (in >> N){
		memset(dp, 'b', sizeof(dp));
		

	
	}

	return 0;
}