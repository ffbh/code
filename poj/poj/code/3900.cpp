#include <iostream>
#include <string>
#include <fstream>
#include <functional>
#include <algorithm>
using namespace std;
int N, M;
bool vis[1000000010];
int w[20], c[20];

int main(){
	ifstream in("C:\\input.txt");
	int T;
	in >> T;
	while (T--){
		in >> N >> M;
		for (int i = 1; i <= N; ++i)
			in >> w[i];
		for (int i = 1; i <= N; ++i)
			in >> c[i];









	}












	return 0;
}